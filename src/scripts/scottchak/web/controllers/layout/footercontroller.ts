import * as angular from "angular";

class FooterController {
  private static $inject: string[] = [];

  constructor() {}
}

angular.module("ScottChak.Web").controller("FooterController", FooterController);
