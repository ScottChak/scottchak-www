import * as angular from "angular";

class HeaderController {
  private static $inject: string[] = [];

  constructor() {}
}

angular.module("ScottChak.Web").controller("HeaderController", HeaderController);
